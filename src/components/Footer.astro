<footer class="site-footer">
  <span class="site-footer__copy">© {new Date().getFullYear()} Ángel G. Cidoncha</span>
  <a class="site-footer__contact" href="mailto:angel@angelcidoncha.es">angel@angelcidoncha.es</a>
</footer>

<style>
  .site-footer {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    width: min(100% - 2.5rem, 84rem);
    margin: 0 auto;
    padding: 1.8rem 0;
    border-top: 1px solid var(--border);
    color: var(--muted);
    font: 600 0.72rem/1 var(--font-mono);
    letter-spacing: 0.02em;
  }

  .site-footer__contact {
    color: var(--muted);
    text-decoration: none;
    transition: color 0.2s ease;
  }
  .site-footer__contact:hover {
    color: var(--text);
  }

  @media (max-width: 560px) {
    .site-footer {
      width: min(100% - 1.5rem, 84rem);
      justify-content: center;
      text-align: center;
    }
  }
</style>
